# Monorepo: workspace packages link to each other; peers resolve from the root.
# Consumer apps using pnpm can set the same so @adobe/ffcpe-custom-node-app-builder peers (hono, hono-openwhisk-adapter, …) install without listing each one.
auto-install-peers=true
strict-peer-dependencies=false
